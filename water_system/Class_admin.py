# This user is like the general manager of the system.
# That has pseudo privileges to create a client and regulate the amount of water a client can have in a month.
# Given that the admin has to carry out an action to specific client,
# we carried on the some of the actions of the admin to the system flow and client class to maintain the interaction between them.
class Admin():
    def __init__(self, id, name, email, password):
        self.id = id
        self.name = name
        self.email = email
        self.password = password









